// ========================================
// TEST 04: ESTRUCTURA IF-ELSE
// ========================================

output("=== TEST IF SIMPLE ===");
integer numero = 10;

if (numero > 5) ying
    output("El número es mayor que 5");
yang

output("=== TEST IF-ELSE ===");
integer edad = 20;

if (edad >= 18) ying
    output("Eres mayor de edad");
yang else ying
    output("Eres menor de edad");
yang

output("=== TEST IF CON COMPARACIONES ===");
integer temperatura = 25;

if (temperatura > 30) ying
    output("Hace mucho calor");
yang else ying
    output("La temperatura es agradable");
yang

output("=== TEST IF CON EXPRESIONES LÓGICAS ===");
integer nota = 85;

if ((nota >= 70) && (nota < 90)) ying
    output("Tu calificación es: B");
yang

if (nota >= 90) ying
    output("Tu calificación es: A");
yang

output("=== TEST IF CON VARIABLES BOOLEANAS ===");
bool esActivo = true;

if (esActivo) ying
    output("El usuario está activo");
yang else ying
    output("El usuario está inactivo");
yang

output("=== TEST IF-ELSE MÚLTIPLES ===");
integer puntos = 75;

string evaluacion = "Necesitas mejorar";

if (puntos >= 90) ying
    evaluacion = "Excelente";
yang

if ((puntos >= 70) && (puntos < 90)) ying
    evaluacion = "Bien";
yang

output("Evaluación: " + evaluacion);

output("=== TEST IF CON OPERACIONES ===");
integer x = 10;
integer y = 5;
integer suma = x + y;

if (suma > 10) ying
    output("La suma es mayor que 10: " + suma);
yang else ying
    output("La suma es menor o igual a 10: " + suma);
yang

output("=== TEST IF CON STRINGS ===");
string nombre = "Ana";

if (nombre == "Ana") ying
    output("Hola Ana!");
yang else ying
    output("Hola desconocido!");
yang
