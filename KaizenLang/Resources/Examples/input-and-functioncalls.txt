// Input builtin and FunctionCall normalization tests

// 1) Simple input usage
string name = input();
output(name);

// 2) Input assigned to typed var (runtime conversion expected)
integer age = input();
output(age);

// 3) Function call with args — ensure AST is normalized
integer add(integer x, integer y) ying
    return x + y;
yang

integer result = add(1, 2);
output(result);

// 4) Call builtin functions as expressions
integer len = length(names); // EXPECT ERROR unless 'length' builtin is implemented (test should tolerate either)

// 5) Mixed: builtins inside function call
output("Hola " + input());

// 6) Ensure FunctionCall AST shape: callee and args — examples for parser to test
// valid: foo(1, 2)
// invalid: foo; (should not be a call)

// 7) Call to input() with extra parentheses and whitespace
string s1 = input ( ) ;
string s2 = input();

// 8) Edge: input used without assignment (should still be allowed)
input();
