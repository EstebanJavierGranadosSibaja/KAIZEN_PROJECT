// Arrays and matrices validation

// 1) Create arrays and read/write
array<integer> a = [10,20,30];
integer x = a[0];
a[1] = 25;

// 2) Matrix creation and arithmetic
matrix<integer> m = [[1,2],[3,4]];
integer m00 = m[0][0];

// 3) Matrix with incompatible inner lengths (should be rejected or defined behavior)
matrix<integer> ragged = [[1,2,3],[4,5]]; // EXPECT ERROR: ragged inner lengths — should be detected by semantic checks

// 4) Operations combining arrays (behavior defined by language)
// e.g., concatenation or element-wise add (if supported) — here we just index into arrays
integer sum = a[0] + a[2];

// 5) Multi-dimensional literals with expressions
matrix<integer> computed = [[1+1, 2*2], [3-1, 8/2]];

// 6) Assignment of array to wrong type
array<string> wrong = a; // EXPECT ERROR: assignment of array<integer> to array<string> should be rejected

// 7) Empty matrix
matrix<integer> mempty = []; // EXPECT ERROR or defined behavior: empty matrix literal may be ambiguous
