int x = 5;
floattt y = 3.14;
string mensaje = "Hola mundo";

int = 5;           // Falta identificador
float 3.14;        // Falta identificador
string "Hola";     // Falta identificador
double;            // Falta identificador

int correcto = 10;
boolean = true;    // Incorrecto
char;              // Incorrecto
float valor;       // Correcto

if  { (x > 3)
    y = y 
    + x;
}

for (int i = 0; i < 5; i = i + 1) {
    print(i);
}

int suma(int a, int b) {
    return a + b;
}