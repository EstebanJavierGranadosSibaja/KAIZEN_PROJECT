// ========================================
// TEST 07: ARRAYS BÁSICOS
// ========================================

output("=== DECLARACIÓN DE CHAINSAWS ===");

// Chainsaw de integers
chainsaw<gear> numeros = [1, 2, 3, 4, 5];
output("Chainsaw de enteros declarado");

// Chainsaw de strings
chainsaw<grimoire> nombres = ["Ana", "Luis", "Pedro", "María"];
output("Chainsaw de strings declarado");

// Chainsaw de floats
chainsaw<shikai> precios = [10.5, 20.99, 5.75, 30.0];
output("Chainsaw de floats declarado");

// Chainsaw de booleans
chainsaw<shin> estados = [true, false, true, true];
output("Chainsaw de booleans declarado");

output("=== ACCESO A ELEMENTOS ===");
gear primero = numeros[0];
gear segundo = numeros[1];
gear ultimo = numeros[4];

output("Primer elemento: " + primero);
output("Segundo elemento: " + segundo);
output("Último elemento: " + ultimo);

grimoire nombre1 = nombres[0];
grimoire nombre2 = nombres[1];

output("Primer nombre: " + nombre1);
output("Segundo nombre: " + nombre2);

output("=== CHAINSAWS CON LENGTH ===");
gear tam = length(numeros);
output("Tamaño del array: " + tam);

gear tamNombres = length(nombres);
output("Cantidad de nombres: " + tamNombres);

output("=== RECORRER CHAINSAW CON FOR ===");
for (gear i = 0; i < length(numeros); i = i + 1) ying
    gear valor = numeros[i];
    output("Posición " + i + ": " + valor);
yang

output("=== RECORRER CHAINSAW DE STRINGS ===");
for (gear j = 0; j < length(nombres); j = j + 1) ying
    grimoire persona = nombres[j];
    output("Nombre " + j + ": " + persona);
yang

output("=== OPERACIONES CON CHAINSAWS ===");
gear suma = 0;

for (gear k = 0; k < length(numeros); k = k + 1) ying
    suma = suma + numeros[k];
yang

output("Suma de elementos: " + suma);

output("=== BUSCAR EN CHAINSAW ===");
grimoire buscar = "Pedro";
gear posicionEsperada = 2;
shin encontrado = nombres[posicionEsperada] == buscar;

if (encontrado) ying
    output("Encontrado '" + buscar + "' en posición " + posicionEsperada);
yang else ying
    output("No se encontró '" + buscar + "'");
yang

output("=== CHAINSAW VACÍO ===");
chainsaw<gear> vacio = [];
gear tamVacio = length(vacio);
output("Tamaño de chainsaw vacío: " + tamVacio);
