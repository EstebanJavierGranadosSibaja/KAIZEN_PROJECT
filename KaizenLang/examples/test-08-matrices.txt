// ========================================
// TEST 08: HOGYOKUS (CHAINSAWS 2D)
// ========================================

output("=== DECLARACIÓN DE HOGYOKUS ===");

// Hogyoku 3x3 de integers
hogyoku<integer> tabla = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];
output("Hogyoku 3x3 declarado");

// Hogyoku 2x3 de floats
hogyoku<float> precios = [
    [10.5, 20.0, 15.75],
    [5.25, 30.0, 8.99]
];
output("Hogyoku de precios declarado");

output("=== ACCESO A ELEMENTOS ===");
integer elem00 = tabla[0][0];
integer elem11 = tabla[1][1];
integer elem22 = tabla[2][2];

output("Elemento [0][0]: " + elem00);
output("Elemento [1][1]: " + elem11);
output("Elemento [2][2]: " + elem22);

integer elem01 = tabla[0][1];
integer elem12 = tabla[1][2];

output("Elemento [0][1]: " + elem01);
output("Elemento [1][2]: " + elem12);

output("=== RECORRER HOGYOKU CON FOR ANIDADO ===");
output("Hogyoku completo:");
for (integer idx = 0; idx < 9; idx = idx + 1) ying
    integer filaRecorrido = idx / 3;
    integer colRecorrido = idx - (filaRecorrido * 3);
    integer valor = tabla[filaRecorrido][colRecorrido];
    output("Posición [" + filaRecorrido + "][" + colRecorrido + "]: " + valor);
yang

output("=== SUMA DE ELEMENTOS DE HOGYOKU ===");
integer sumaTotal = 0;

for (integer indice = 0; indice < 9; indice = indice + 1) ying
    integer filaSuma = indice / 3;
    integer colSuma = indice - (filaSuma * 3);
    sumaTotal = sumaTotal + tabla[filaSuma][colSuma];
yang

output("Suma total de elementos: " + sumaTotal);

output("=== BUSCAR ELEMENTO EN HOGYOKU ===");
integer buscar = 5;
integer filaObjetivo = 1;
integer colObjetivo = 1;
bool encontrado = tabla[filaObjetivo][colObjetivo] == buscar;

if (encontrado) ying
    output("Encontrado " + buscar + " en [" + filaObjetivo + "][" + colObjetivo + "]");
yang else ying
    output("No encontrado");
yang

output("=== HOGYOKU IDENTIDAD 3x3 ===");
hogyoku<integer> identidad = [
    [1, 0, 0],
    [0, 1, 0],
    [0, 0, 1]
];

output("Diagonal principal:");
for (integer d = 0; d < 3; d = d + 1) ying
    integer diagonal = identidad[d][d];
    output("Posición [" + d + "][" + d + "]: " + diagonal);
yang

output("=== HOGYOKU DE STRINGS ===");
hogyoku<string> nombres = [
    ["Ana", "Luis", "Pedro"],
    ["María", "José", "Carmen"]
];

output("Primera fila:");
output(nombres[0][0] + ", " + nombres[0][1] + ", " + nombres[0][2]);

output("Segunda fila:");
output(nombres[1][0] + ", " + nombres[1][1] + ", " + nombres[1][2]);
